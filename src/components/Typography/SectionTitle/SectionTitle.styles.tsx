import styled from 'styled-components';
import { fontSize, margin } from 'styles';

export const SectionTitle = styled.h2`
  color: var(--text-color);
  font-size: ${fontSize.xl};
  margin-bottom: ${margin.xl};
  text-align: center;
  font-weight: 600;
`;
